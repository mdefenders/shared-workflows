name: Publish Docker Image

on:
  workflow_call:

jobs:
  quality-check:
    if: startsWith(github.ref, 'refs/heads/feature/')
    name: Code Quality Gates
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # --- LINT CHECK ---
      - name: Lint code (ESLint)
        run: npx eslint .

      # --- FORMAT CHECK ---
      - name: Check formatting (ESLint)
        run: npm run format:check

      # --- UNIT TESTS + COVERAGE ---
      - name: Run tests with coverage (Jest)
        run: npm run test -- --coverage > coverage/output.txt

      - name: Check test coverage threshold
        run: |
          COVERAGE=$(cat coverage/output.txt | grep 'All files' | awk '{print $4}' | tr -d '%')
          echo "Current coverage: $COVERAGE%"
          if (( $(echo "$COVERAGE < 80" | bc -l) )); then
            echo "Coverage below threshold (80%)"
            exit 1
          fi